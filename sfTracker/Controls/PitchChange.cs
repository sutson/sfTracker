namespace sfTracker.Controls
{
    /// <summary>
    /// Enum for note transposition values.
    /// </summary>
    public enum PitchChange
    {
        IncreaseSemitone = 1,
        DecreaseSemitone = -1,
        IncreaseOctave = 12,
        DecreaseOctave = -12,
    }
}